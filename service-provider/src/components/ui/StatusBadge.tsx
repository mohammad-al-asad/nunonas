import { cn } from "@/lib/utils";

export function StatusBadge({ status }: { status: string }) { const normalized = status.toLowerCase().replace(/[_-]/g, " "); return <span className={cn("inline-flex rounded-lg px-3 py-1 text-xs font-bold capitalize", ["confirmed", "complete", "completed", "published", "active", "paid"].includes(normalized) && "bg-emerald-50 text-emerald-700", ["pending", "draft", "check in"].includes(normalized) && "bg-amber-50 text-amber-700", ["cancelled", "canceled", "archived", "inactive", "failed"].includes(normalized) && "bg-rose-50 text-rose-700", !["confirmed", "complete", "completed", "published", "active", "paid", "pending", "draft", "check in", "cancelled", "canceled", "archived", "inactive", "failed"].includes(normalized) && "bg-slate-100 text-slate-600")}>{status}</span>; }
